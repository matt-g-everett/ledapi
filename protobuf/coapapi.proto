syntax = "proto3";
package ledapi;

message Frame {
    bytes frame_data = 1;
}

message Config {
    enum Mode {
        OFF = 0;
        FIXED_FRAME = 1;
        SWISH_RAINBOW = 2;
    }
    Mode mode = 1;
    Frame fixed_frame = 2;
}

message FrameSet {
    repeated Frame frames = 1;
}
